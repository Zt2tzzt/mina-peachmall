@import "./styles/iconfont.wxss";

/* 在微信小程序中，不支持通配符 */
page,view,text,swiper,swiper-item,image,navigator {
  padding: 0;
  margin: 0;
  box-sizing: border-box;
}

/* 
主题颜色通过变量来实现
1. less中存在变量
2. 原声的css和wxss也是支持变量的
 */
page {
  /* 定义主题颜色 */
  --themeColor: #008000;
}

image {
  width: 100%;
}